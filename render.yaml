services:
  - type: web
    name: projecttracker-api
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn projecttracker-api.wsgi:application"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: false